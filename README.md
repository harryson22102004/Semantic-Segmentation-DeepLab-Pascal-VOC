# Semantic Segmentation — DeepLab + Pascal VOC
Pixel-wise semantic segmentation using DeepLab-v3+ on Pascal VOC 2012.
### ■ Research Highlights
- **Atrous Spatial Pyramid Pooling**: Applies dilated convolutions at multiple rates to capture multi-scale - **Encoder–Decoder Design**: Combines a ResNet-101 encoder with a lightweight decoder that refines object bo- **Algorithms Focus**: Reports per-class and mean IoU on VOC 2012 val set, analysing failure cases on thin **Tech Stack:** Python, `PyTorch`, `torchvision`.
